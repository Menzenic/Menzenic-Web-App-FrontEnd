import { SignIn, SignUp } from "../../components"

const Auth = () => (
    <div>
        {/* <SignIn onClose={() => {}} /> */}
        <SignUp onClose={() => {}} />

        <div
            className="flex-1 bg-cover"
        ></div>
    </div>
)

export default Auth
